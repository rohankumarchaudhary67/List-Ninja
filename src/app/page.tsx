import { Button } from "@/components/buttons/Button";

export default function Home() {
  return (
    <>
      <div className="flex justify-center items-center h-screen w-screen">
        <Button size="default" variant="default">Connect with Google</Button>
      </div>
    </>
  )
}
