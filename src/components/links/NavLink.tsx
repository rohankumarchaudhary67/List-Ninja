import { LinkProps } from "next/link";
import { forwardRef } from "react";

const NavLink = forwardRef<HTMLLinkElement, LinkProps>(
    ({}, ref) => {
        return(
            <>
            
            </>
        )
    }
)

export {NavLink}